# 更新日志

## v0.72 - 2026-06-07
安全审计全面修复、数据统计仪表盘上线、举报通知全链路打通

### 安全修复（高危）
- 修复 XSS 注入漏洞：所有头像 URL 和用户输入统一 `escapeHtml + sanitizeUrl` 双重转义。
- 修复路径穿越漏洞：上传文件名过滤 `../` 等特殊字符，阻止危险扩展名（`.php`、`.exe`、`.asp` 等）。
- 修复 CSRF 漏洞：API 端统一 `verifyToken` JWT 鉴权中间件校验。
- 修复敏感信息泄漏：统一脱敏处理，不再向前端暴露内部错误细节。
- 新增安全响应头：CSP、X-Frame-Options、X-XSS-Protection、X-Content-Type-Options、Referrer-Policy。
- 新增 API 频率限制中间件 `rateLimit()`，防止 DoS 攻击。
- 新增 CORS 白名单校验，仅允许可信域名跨域访问。

### 新增
- **数据统计仪表盘**（管理员控制台）：
  - 8 个总览数据卡片：用户数量、帖子数量、评论数量、点赞数量、照片数量、访问总次数、被攻击次数、API 防火墙拦截次数
  - 6 种可切换的每日趋势柱状图：访问量 / 攻击量 / 发帖量 / 评论量 / 点赞量 / 新用户
  - 攻击类型分布水平条形图（可直观查看 RATE_LIMIT、CSRF、CORS 等攻击占比）
  - 每日数据明细表格，支持 `start/end` 日期范围筛选
  - 用户访问明细表格：每个用户的总访问次数、今日访问次数、最近登录时间、注册时间
  - 数据缓存机制（60 秒 TTL），支持手动刷新按钮
- **后端统计 API 端点**：`/admin/stats`（汇总）、`/admin/stats/daily`（每日）、`/admin/stats/refresh`（刷新缓存）、`/admin/stats/users`（用户访问明细）
- **用户访问自动记录**：页面加载、登录、注册时自动调用 `/api/log-user-visit` 记录用户访问日志（`__user_visit__` 标记），同步更新最近登录时间
- **管理员回复通知**：处理举报时自动通过私信（`__dm__` 标记）通知举报人，包含举报处理结果原文
- **安全测试脚本** `security-test.js`：自动模拟 XSS 注入、路径穿越、CSRF、高频请求等 8 类攻击测试，验证修复效果

### 修复
- 修复举报数据错误显示在帖子列表中的问题：所有 `posts` 表查询增加 `.neq("media_type", REPORT_MARKER)` 过滤。
- 修复管理员回复举报后举报人未收到通知的问题：`sendAdminDm` 在举报回复、内容删除、用户封禁等操作后自动触发。
- 修复统计模块 `dailyAttacks` 日期聚合逻辑始终返回空字符串的 bug。
- 修复私信新消息仍使用旧加载动画的问题，统一为照片墙同款加载动画。

### 优化
- 数据统计页采用主渲染 + 异步加载策略：汇总卡片和图表优先渲染，用户访问明细异步加载不阻塞。
- 统计 API 使用内存缓存减少数据库查询压力（`statsCache`，60 秒刷新）。
- 全局 DOM 渲染性能优化：合并 DOM 操作、减少重排重绘。
- 头像 URL 访问增加额外的 4 层安全性检查（类型校验、尺寸限制、CORS 验证、危险字符过滤）。
- 文件上传增加完整的类型白名单和大小校验，拒绝危险 MIME 类型。

### Remade
- 重做后端安全架构：JWT 鉴权 + 速率限制 + 输入校验 + 错误脱敏 + 安全响应头五层防护体系。
- 重做管理员统计页面布局与数据展示逻辑，从无到有搭建完整的统计可视化模块。

## v0.71 - 2026-06-06
安全审计全面修复与黑名单管理上线

### 安全修复
- 移除前端硬编码管理员密码，改为后端 API 和环境变量认证。
- 新增 CORS 白名单、安全响应头、API 频率限制和输入长度校验。
- 错误信息统一脱敏，不再向前端暴露内部细节。
- 增强密码策略，注册密码最小长度提升到 6 位。
- 文件上传增加类型和大小校验。

### 新增
- 黑名单管理后端接口与管理后台界面全套上线。
- 用户限制状态轮询，拉黑、封禁、禁言可即时生效。

### Remade
- 后端管理 API 重构为 JWT 鉴权、频率限制、输入校验、错误脱敏四层防护架构。

## v0.70 - 2026-06-05
后台管理大更新

### 新增
- 用户列表 UI 改为卡片式网格布局。
- 支持按状态筛选、按注册时间/最近登录/帖子数排序、实时搜索用户名。
- 拉黑封禁表和禁言表补充解除时间列。

### 修复
- 修复用户列表最近登录时间显示旧数据的问题。
- 修复管理面板初始化未加载 bans/mutes 数据的问题。

### Remade
- 重做后台用户列表 UI 和交互体验，整体更直观。

## v0.69 - 2026-06-04
我的页互动收口

### 新增
- 我的页点赞记录和评论记录改为首页只预览 1 条。
- 更多点赞内容和更多评论内容统一改成二级弹层。

### 优化
- 评论记录整条可直接查看帖子，主按钮改成删除评论。
- 点赞记录缩略图位置收紧到文案右侧，信息关系更清晰。

### Remade
- 重做我的页互动入口、记录弹层和详情入口关系。

## v0.68 - 2026-06-04
帖子交互修复与乱码全面清理

### 修复
- 置顶与取消置顶改为即时重排。
- 帖子卡片移除重复的公开/私密角标，只保留统计行状态。
- 修复预览、上传和部分动态刷新细节问题。
- 全面修复站内大量中文乱码显示。

## v0.67 - 2026-06-02
性能大幅优化

### 优化
- 花草圈圈 Canvas 动画大幅降耗，减少模糊、分段、粒子和残影。
- `escapeHtml` 和文本修复链路改为更轻量的字符串方案。
- 全局交互节流与加载动画降耗同步上线。

## v0.66 - 2026-06-01
加载动画主题升级

### 新增
- 上线花草圈圈主题加载动画，替换旧的通用加载视觉。

## v0.65 - 2026-06-01
乱码清理、点赞修复与帖子状态修复

### 修复
- 修复点赞按钮文案与状态逻辑错误。
- 修复多处 Toast、按钮、详情页、版本日志里的中文乱码。
- 修复公开/私密切换和置顶功能的持久化问题。

## v0.64 - 2026-05-31
乱码修复与动画升级

### 修复
- 修复帖子卡片、统计区、帖子详情等多处中文乱码。

### 优化
- 加载动画增加粒子、光环、符文环和镜面核心视觉。

## v0.62 - 2026-05-30
功能优化与 Bug 修复

### 优化
- 筛选控件整合为折叠式筛选面板。
- 清理帖子举报按钮及相关前端残留。

### 修复
- 修复编辑帖子时公开/私密不真正生效的问题。
- 修复帖子置顶功能不生效的问题。
