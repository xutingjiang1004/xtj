# 更新日志

## v0.62 - 2026-05-30 — 功能优化与Bug修复

### 优化
- 筛选功能优化：将内联筛选控件整合为折叠式"筛选"按钮面板，支持活跃筛选计数徽章
- 移除帖子举报按钮及全部相关代码，清理前端残留

### 修复
- 修复编辑帖子时公开/私密选项不真正生效的问题
- 修复帖子置顶功能不生效的问题

---

## v61 - 2026-05-30 — 项目清理与全面检查

- 删除所有冗余备份文件、临时修复脚本和测试脚本（js备份、scripts目录、root fix/test等）
- 全面检查：HTML引用完整性、JS语法（全部通过）、乱码扫描、后端服务验证

---

## 2026-05-28 — 核心功能修复与照片墙预览优化

### 修复
- 修复编辑帖子公开/私密不真正生效问题
- 修复统计详情泄露私密帖子互动
- 修复照片预览双击缩小/双指缩放不稳定

### 优化
- 照片墙预览新增双指缩放
- 标记废弃函数避免误修改
- upload.js select 字段完整性提升

---

## 2026-05-27 — 功能修复与稳定性提升

### 修复
- 修复举报按钮点击无响应问题
- 修复举报提交字段名匹配，添加 fallback 机制
- 修复通知开关 localStorage key 不一致
- 修复统计详情泄露私密帖子互动
- 修复帖子详情页无私密权限检查
- 修复发帖文件上传未检查错误

### 优化
- 照片墙缩略图加载速度提升
- 去除 index.html UTF-8 BOM

## 2026-05-26 — 照片墙功能全面优化

### 修复
- **修复图片上传失败问题**：移除了 Supabase 不支持的 `onUploadProgress` 参数，该参数导致上传接口直接报错。改用定时器模拟进度动画，上传改为标准 Supabase API 调用，确保上传稳定可靠
- **修复长按下载闪退问题**：移除了复杂的 `ReadableStream` 流式读取和 `AbortController` 机制，改用简洁的 `response.blob()` 方式下载，避免移动端流读取兼容性问题。添加完善的 try-catch 降级方案（自动跳转新窗口下载）
- **修复底部分享按钮不显示**：移除了 `!important` 对按钮 `background`/`border` 的强制覆盖，恢复各按钮独立样式

### 新增
- **上传进度弹窗点击外部关闭**：上传进度显示时，点击遮罩空白区域即可关闭弹窗
- **系统级分享功能**：优先调用 `navigator.share()` 原生分享，降级为复制链接

### 优化
- **全屏预览底部按钮UI**：统一采用极简半透明风格，移除磨砂、高光、加粗效果，透明度降至 8%-10%，悬停时升至 22%-25%
- **简介"i"信息模块**：外部背景完全透明，内部内容区使用 10% 白色磨砂玻璃效果（`rgba(255,255,255,0.10)` + `blur(20px)`），不再是之前的黑色半透明遮罩
- **照片墙整体透明度**：所有按钮和交互元素统一调整为低透明度，提升视觉通透感和现代感
- **上传/下载进度动画**：优化进度条光泽流动动画，更流畅自然

### 技术细节
- 上传流程分阶段显示进度：处理图片(10%) → 创建缩略图(30%) → 上传主图(40%-80%) → 上传缩略图(85%) → 更新数据(95%) → 完成(100%)
- 下载失败自动降级为 `<a target="_blank">` 新窗口打开，确保跨浏览器兼容
- 所有 DOM 操作包裹 try-catch 防护