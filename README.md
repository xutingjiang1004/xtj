# 更新日志

## v0.0.52 - 2026-05-25
### 照片预览体验全面优化 + UI交互升级

#### 1. 照片预览左右切换箭头
- 在照片预览界面左右两侧添加简洁风格的切换图标（SVG chevron 箭头）
- 点击箭头可切换上一张/下一张照片，支持边界保护（首张隐藏左箭头，末张隐藏右箭头）
- 液态玻璃风格设计：backdrop-filter: blur(16px) + 微边框 + 阴影
- hover 缩放动画 + active 按压反馈，移动端响应式适配（38px）

#### 2. 多层次入场动画
- 背景淡入 → 照片缩放出现（0.95→1）→ 关闭按钮滑入 → 导航箭头缩放出现 → 信息栏滑入
- 使用弹性缓动曲线 cubic-bezier(0.16,1,0.3,1)，过渡更加自然流畅
- 各元素渐次出现，形成有层次感的入场序列

#### 3. 滑动性能深度优化
- 预加载数量从 ±3 张优化为 ±1 张，减少内存占用约 66%
- RAF 调用添加防抖机制（cancelAnimationFrame），避免调用累积
- 预加载时机延迟到动画完成后执行，避免资源竞争
- CSS 添加 contain: layout paint style、will-change: transform 等 GPU 加速属性

#### 4. 照片墙图片加载优化
- 修复懒加载时机问题：等待图片加载完成后再做过渡动画
- 添加图片淡入效果（模糊+透明 → 清晰+不透明），加载过程更平滑

#### 5. 液态玻璃透明度提升
- profile 用户卡片：rgba(255,255,255,0.30) + 22px 模糊 + 160% 饱和度
- 照片墙头部：透明度降低至 0.35（深色模式 0.22）
- 预览信息栏：增强毛玻璃效果和渐变层次

#### 6. 简介页 UI 排版优化
- 标题采用渐变绿色设计（#059669 → #34d399）
- 设置项添加 hover 微动效（translateX(3px)）和毛玻璃背景
- 切换开关添加弹性过渡曲线

#### 7. 举报按钮事件委托修复
- 移除内联 onclick，改用 document.addEventListener('click') + closest() 事件委托
- 从根本上解决动态渲染内容的事件绑定问题

## v0.0.51 - 2026-05-25
### 举报按钮修复 + 双指放大性能优化 + 照片上传自动压缩

#### 1. 举报按钮修复
- 将举报按钮直接嵌入帖子模板HTML（renderFeedWithAvatars 和 appendMorePosts），替代脆弱的DOM打补丁方式
- 移除features.js中的MutationObserver补丁代码，按钮随帖子初始加载一并渲染，杜绝消失问题
- 举报按钮右对齐置底，通过inline onclick调用window.openReport，兼容所有设备和屏幕尺寸

#### 2. 照片全屏预览双指放大性能优化
- CSS层面启用GPU硬件加速：backface-visibility: hidden + transform: translateZ(0) + will-change: transform
- 手势系统重构：预分配PinchPre对象避免每帧Array.from分配，降低GC压力
- 新增屏幕刷新率自动检测（rAF中值法），自适应120Hz/90Hz/60Hz帧预算
- viewport中心点预计算缓存，减少每帧布局查询

#### 3. 照片上传自动压缩
- 新增compressToMaxSize函数：文件>10MB时自动压缩至~10MB，多级降级策略（2560→2048→1920→1280→800像素）
- 100MB超大型照片也能自动压缩后上传，不再直接拒绝
- 压缩失败时回退策略：≤50MB直接上传原文件，>50MB且压缩失败则跳过
- 压缩前后尺寸均记录（fileSize + originalSize），数据透明可追溯
- Supabase免费版限制已确认：文件存储1GB，单文件50MB，月带宽5GB

## v0.0.50 - 2026-05-25
### 照片墙功能全面完善 + 预览交互优化

#### 1. 照片墙功能完善
- 新增按日期、名称、热度三种条件的筛选排序功能，用户切换后立即响应并正确重新排列照片顺序
- 修复相册视图显示"暂无照片"的空白问题，点击相册按钮后正确加载并显示对应相册内容
- 导航栏随上下滑动自动隐藏/显示，向下滑动浏览照片时平滑隐藏，向上滑动时重新显示，底部不遮挡内容

#### 2. 照片预览交互优化
- 修复全屏预览下单点退出与双击放大的冲突问题，单点触发退出、双击触发放大，两种操作互不干扰
- 删除按钮图标由"x"替换为垃圾桶SVG图标，与右上角关闭按钮清晰区分
- 优化左右滑动预览时的图片加载策略，消除黑屏：实现图片缓存系统（含加载状态追踪），延迟加载前后图片避免竞争带宽，预加载范围扩展至前后3张
- 图片加载时显示脉冲动画背景（渐变+呼吸光效），替代纯黑背景，消除用户感知的卡顿

## v0.0.49 - 2026-05-25
### 功能模块全面升级：用户管理 & 内容安全 + 照片墙增强 + 性能优化

#### 1. 用户管理与内容安全模块
- 举报功能：新增完整举报系统，支持8种举报类型分类（垃圾广告/骚扰/不雅内容/虚假信息/暴力/仇恨言论/版权/其他），用户可填写举报理由并上传证据截图，举报数据存储至 Supabase reports 表
- 封禁机制：构建用户封禁系统，管理员可在后台对用户执行临时封禁（设置时长小时数）或永久封禁，被封禁用户登录时自动拦截并提示原因，过期封禁非常抱歉给您带来了不好的体验！这次是我们的工作失误，我们深感愧疚。为了弥补您的损失，这边给您申请 8 元 或 包运费 为您换一下，真的非常抱歉倍亲，我们后续会严格改进，避免类似问题再次发生。
户，黑名单用户无法登录系统
- 管理后台同步：admin.html 新增"举报管理"、"封禁管理"、"黑名单"、"照片管理"四个选项卡，所有操作实时同步至 Supabase 数据库
- 数据库扩展：新增 reports、bans、blacklist、photos 四张数据表及 RLS 策略，含 is_user_banned()、is_user_blacklisted()、auto_expire_bans() 实用函数

#### 2. 照片墙功能增强
- 批量上传：支持同时选择多张图片上传（multiple 属性），实时显示每张图片的上传进度条与状态（上传中→完成/失败），自动生成缩略图并保存至 posts 表和 photos 表
- 按日期自动分类相册：新增相册视图模式，按上传日期自动分组整理照片，每个相册显示封面图、日期（含星期）、照片数量，点击相册卡片进入该日期照片预览
- 自动生成封面：自动选取最佳照片作为相册封面（综合浏览数 + 上传时间评分）
- 多种排序方式：支持下拉选择器切换排序方式（按日期↓ / 按日期↑ / 按名称 / 按大小 / 按热度），排序同时影响网格视图和相册视图

#### 3. 系统性能优化升级
- 虚拟滚动列表：照片墙照片超过30张时自动启用虚拟滚动，仅渲染可视区域内（含上下缓冲4行）的图片，使用 position: absolute + requestAnimationFrame 节流，大幅降低DOM节点数
- 服务端分页功能：帖子流使用 Supabase .range() 分页查询（每页20条），IntersectionObserver 监听哨兵元素自动加载下一页，API 端点 GET /posts/page
- 懒加载策略：统计页面仅在 Profile 选项卡激活时通过 MutationObserver 懒加载注入数据，避免首屏加载不必要的统计计算
- 数据获取重构：初始加载仅拉取第一页帖子（20条）+ 点赞/评论，2秒后在后台静默拉取剩余数据（上限200条），避免一次性加载全量数据

#### 4. 后端 API 接口
- render-api/server.js 新增完整 REST API：举报提交/查询/处理（POST/GET/PUT /report）、封禁管理（POST /ban、GET /ban/check/:user_name、PUT /ban/:id/unban）、黑名单管理（POST/DELETE /blacklist）、照片管理（GET/POST/DELETE /photos）、分页帖子（GET /posts/page）
### 双分辨率策略（Thumbnail + Full Resolution）
- 重构 handlePhotoUpload：上传原图的同时，利用 compressImage 生成 400x400 极度压缩的 JPEG 缩略图，并上传至 Supabase Storage 的 thumbs/ 目录
- 缩略图 URL 存入 posts 表的 content JSON 字段 `{ type: 'photo_wall', thumb: '...' }`，normalizePhotoWallRow 新增 thumbUrl 字段解析
- 网格渲染（renderPhotoWall / renderPhotoWallWithoutReload）img src 优先使用缩略图 thumbUrl，无缩略图的旧照片自动回退到原图 imageUrl
- 全屏预览（ppSetTrackImages / openPhotoPreview）始终加载高清原图 imageUrl，点击进入时瞬间加载高分辨率
- 缩略图上传失败不阻塞主流程：自动降级为纯原图模式，用户无感知

## v0.0.47 - 2026-05-25
### 陀螺仪视差效果（Gyroscope Parallax）
- 全屏照片预览中新增 deviceorientation 监听，倾斜手机/平板时照片和遮罩产生轻微 3D 悬浮错位感
- 处理 iOS 13+ 权限问题：在 ppOpenPreview 中通过 DeviceOrientationEvent.requestPermission 请求授权
- 视差算法：beta（前后倾斜）和 gamma（左右倾斜）映射为 ±15px 像素偏移
- 关键性能点：deviceorientation 事件只更新 target 变量，在专用 rAF 循环中使用 Lerp 插值（speed=0.1）平滑更新 DOM
- 视觉层次：照片应用正向 translate3d，底部信息栏（用户名/浏览量）应用反向 0.35 倍偏移，形成景深感
- 关闭预览时 cleanStopGyro：移除 deviceorientation 监听器、取消 rAF 循环、复位所有偏移，杜绝后台耗电

## v0.0.46 - 2026-05-25
### 120Hz 弹簧物理引擎（Spring Physics）
- 移除照片墙轨道 ppTrack 所有 CSS transition，将动画控制权完全交给 JS，匹配 120Hz 刷新率
- 实现轻量级弹簧动画循环，基于胡克定律（F_spring = -k * x）与阻尼力（F_damping = -c * v）
- 重写 ppSnapTo 函数：使用 requestAnimationFrame 循环 + Euler 积分，每帧通过弹簧算法计算新的 ppTrackDrag 并应用到 transform
- 物理参数 Tension=180, Friction=18（接近临界阻尼），提供 iOS UIScrollView 级别的"干脆、跟手、丝滑"手感
- 处理所有边角情况：拖拽手势中断弹簧时释放导航锁、轻触弹簧中时立即取消防止跳动、pointercancel 时清理弹簧状态

## v0.0.45 - 2026-05-25
### 深度修复与优化
采用 Gemini 的专业修复方案，完全重写了多个核心功能：

1. 彻底消除照片左右滑动黑屏：核心思路是先执行物理滑动动画（利用已经提前预加载在左右槽的图片），等 300ms 动画结束后再悄悄重新排布三个槽位并拉回轨道，视觉上完全无缝
2. 优化缩放卡顿：新增 requestAnimationFrame 把图像变换交给 GPU 批量渲染；优化 classList 更新逻辑，避免手指微动时频繁触发重排
3. 解决单指滑动拖影：新增 requestAnimationFrame 把轨道拖拽也加入 GPU 渲染队列，保证单指左右拖轨道时同样丝滑

### 额外 5 项优化任务
- 任务1：修复全屏图片放大后的无边界拖拽 Bug：计算真实溢出宽高，利用 Math.min 和 Math.max 将图片的 tx 和 ty 钳制在可视视口范围内
- 任务2：修复头像存储的性能隐患（Base64 反模式）：重构 handleAvatarUpload，将头像文件上传到 Supabase Storage 的 avatars/ 目录，只保存 URL 到数据库和 localStorage
- 任务3：优化 WebSocket 与长轮询的资源消耗：将默认轮询间隔拉长到 5 分钟（300000ms），聊天对话时为 60000ms，大幅降低数据库请求压力
- 任务4：彻底修复 iOS 键盘弹出的遮挡与跳动：使用 100dvh 替代旧的 --vh 计算方案，移除旧的 iOS 调整代码，让浏览器原生处理键盘高度变化
- 任务5：为 Feed 流增加基础的分页加载：设置每页 20 条，使用 IntersectionObserver 实现触底自动加载下一页的无限滚动

## v0.0.44 - 2026-05-25
### 修复
- 彻底修复照片左右滑动黑屏问题：重构 ppNavigatePhoto 逻辑，在滑动动画开始前先正确重新排布三个槽的图片，确保用户在滑动过程中看到连续的图片而非黑屏
- 修复放大图片后无法单击返回的问题：修改 document.addEventListener('click') 回调，现在点击图片区域时，如果图片已放大则先缩小到 1 倍，未放大则直接关闭预览
- 修复照片全屏预览重叠显示问题：移除导致重叠的 View Transitions API 相关代码，仅保留 CSS 淡入动画
- 修复照片删除后刷新重新出现的问题：新增 photoWallDeletedKey 本地删除 ID 追踪机制，加载时自动过滤已删除照片；删除时同步删除云端记录和 Storage 文件
- 增强健壮性：添加 overlay 空值守卫，清理无效删除记录等

## v0.0.43 - 2026-05-25
### 性能与体验优化
- GPU渲染降级（移动端适配）：移动端触摸设备上自动降低毛玻璃模糊强度，显著减少发热与卡顿
  - 基础毛玻璃：从 blur(38px) 降至 blur(12px)
  - 底部导航栏：完全移除 backdrop-filter
  - 照片预览遮罩层：降至 blur(4px)
  - 使用媒体查询 @media (hover: none) and (pointer: coarse) 智能检测触摸设备
- 主线程阻塞修复：compressImage 函数优先使用 createImageBitmap 将图片解码/缩放移出主线程，避免大图压缩时浏览器假死，不支持的浏览器自动回退到传统方法
- 原生级视图过渡：View Transitions API 实现照片预览打开/关闭的丝滑动画，自动降级兼容
- 沉浸式环境光背景：Canvas 提取照片主色调，全屏预览背景随照片切换动态变化（辐射渐变）
- 状态与DOM解耦：新增 appState 命名空间、safeText 安全转义函数、虚拟滚动规划注释，增强代码安全性和可维护性

## v0.0.42 - 2026-05-25
### Bug 修复
- 修复删除按钮位置错误：从照片首页卡片右上角移除，仅在照片预览内部左下角显示，仅上传者可见
- 修复删除按钮图标：从垃圾桶图标改为叉叉图标，语义更清晰
- 修复点击删除后照片仍存在的问题：统一使用 String() 类型比较避免 ID 不匹配；先删除云端数据再删除本地数据；删除后正确关闭预览并刷新照片墙
- 修复左右滑动照片黑屏：DOM 操作移至回调内执行，避免 reflow 干扰 CSS 过渡；预加载扩大到前后各 2 张；检查图片加载完毕后再重置轨道位置；预览图片添加 loading="eager"
- 去除底部导航栏液态玻璃效果：恢复为无背景无边框的纯图标文字样式，保持简洁

## v0.0.41 - 2026-05-25
### 更新内容
- 照片墙UI/UX全面增强
  - 图片淡入加载（Blur-in Lazy Load）：图片进入视口时从模糊到清晰渐显，使用 IntersectionObserver 懒加载，减少初始带宽占用
  - 卡片悬浮微动（Hover Lift）：鼠标/手指经过照片卡片时卡片轻轻浮起（translateY(-4px) + scale(1.02)），阴影加深，提供即时的交互反馈
  - 交错入场动画（Staggered Entrance）：照片卡片首次加载时从底部依次弹入，每张延迟 50ms，类 iOS 相册入场效果
  - 骨架屏加载（Skeleton Cards）：数据加载期间显示 6 个脉冲动画灰色占位卡片，替代空白网格
  - 空状态引导优化：无照片时显示大号上传按钮"成为第一个分享照片的人"，降低操作门槛
  - 预览页面指示点（Page Dots）：全屏预览顶部居中显示圆点，当前照片高亮，一眼知晓位置和总张数
  - 照片删除功能：预览模式左下角显示删除按钮，点击弹出液态玻璃确认弹窗，仅照片上传者可见
  - 快捷分享按钮：预览模式右下角链接图标，支持原生分享 API / 剪贴板复制 / 兜底复制，复制后 toast 提示
- 液态玻璃效果覆盖
  - 照片墙标题栏：sticky 吸顶液态玻璃模糊，半透明背景 + 内阴影高光 + 圆角卡片风格
  - 上传按钮：从纯色渐变改为绿色调液态玻璃（绿色微透明底 + blur(12px) + 绿色描边），暗色模式亮绿配色
  - 消息通知横幅：顶部居中滑入，高强度液态玻璃（blur(28px) + saturate(200%)），头像增大至 48px，双层内阴影模拟玻璃厚度感

## v0.0.40 - 2026-05-24
### 更新内容
- UI视觉优化
  - 底部导航栏去框融合：移除背景、边框、阴影，仅保留四个按钮可见，按钮间区域可穿透点击
  - 统一面板/页面背景为中性色（浅灰/深灰），移除绿色色调，解决iOS底部绿色透显问题
- 照片墙功能增强
  - 新增全屏浏览左右滑动切换图片功能，支持手势拖拽导航
  - 首尾边界处理：第一张不能左滑，最后一张不能右滑，带阻力反馈和弹回动画
  - 取消过渡闪烁：修复切换图片时的位置跳跃和闪白bug
  - 双指缩放优化：移除RAF批处理延迟，直接应用transform实现原生级跟手流畅度
  - 整体滑动流畅度优化：will-change、transition精细化控制
- 响应式适配
  - 平板（768px+）：容器满宽、更大的间距和字体、文章卡片居中
  - 桌面（1024px+）：照片墙3列、文章卡片更宽、字体更大
  - 宽屏（1280px+）：照片墙4列、更多留白
  - 横屏手机优化：缩小底部导航栏占用空间
- 代码清理
  - 删除遗留的i18n翻译代码（translations字典、translatePage函数、语言选择UI）
  - 精简syncProfileUser等函数，移除对翻译字典的依赖
  - 移除profile-lang-tabs相关CSS样式
- Bug修复
  - 修复管理员发公告时在帖子流中自动创建帖子的bug（feed查询未过滤ANN_MARKER）

## v0.0.39 - 2026-05-20
### 更新内容
- 照片墙全面重构：base64 存储 --> Supabase Storage 云端存储
  - 彻底解决 localStorage 配额限制导致照片上传静默失败的问题
  - 照片原画质上传至 Supabase Storage，无容量上限（单张限 50MB）
  - 只将元数据（URL、用户名、时间戳）存入 localStorage，大幅降低存储占用
  - 自动清理旧的 base64 垃圾数据，释放存储空间
  - 上传机制与发帖上传图片完全一致，复用现有 Supabase 存储桶
- 照片墙渲染修复
  - 修复独立 IIFE 作用域无法访问 `escapeHtml` 导致的渲染崩溃
  - 将 `escapeHtml`、`sb`（Supabase 客户端）暴露到 `window` 全局
- 照片墙功能完善
  - 上传限制从 20MB 放宽至 50MB
  - 最大保存照片数从 50 提升至 100 张
  - 简化数据存储逻辑，移除非必要的 Blob 大小检查
- 底部导航栏动画修复
  - 修复点击「我的」tab 首次不播放点击动画的问题
  - 将动画触发逻辑移至 `switchDockTab` 函数内部，避免因早退 return 导致动画未执行

## v0.0.38 - 2026-05-18
### 更新内容
- 代码清理与精简
  - 彻底移除雅思单词学习系统全部代码（CSS样式、JS逻辑、HTML结构）
  - 删除设置页中的英语/韩语切换选项，仅保留中文
  - 清理所有废弃的翻译文本和语言切换相关JS逻辑
  - 修复scroll handler中对旧vocab-container的错误引用

## v0.0.37 - 2026-05-18
### 更新内容
- 雅思单词版块全面重做为照片墙（相册功能）
  - 完全替换panelAi面板为照片墙HTML结构，移除所有单词学习界面
  - 每位用户可独立上传照片（base64存储至localStorage，单张限制20MB）
  - 横排5张网格布局（grid-template-columns: repeat(5, 1fr)），竖排无限滚动排列
  - 照片卡片hover时显示发布者名称、发布时间、浏览量
  - 点击任意照片进入全屏预览：固定定位遮罩层，原画质居中展示
  - 预览页显示发布用户、发布时间、浏览量（点击自动+1计数）
  - 照片按上传时间倒序排列（最新在前），支持智能时间格式化
  - 完整CSS样式：照片墙容器、5列网格、卡片交互、全屏预览、深色模式适配
  - 预览层点击背景区域或关闭按钮均可关闭

## v0.0.36 - 2026-05-13
### 更新内容
- 彻底修复所有问题，实现极致的液态玻璃效果
  - 给单词页面添加复杂渐变纹理背景，让backdrop-filter能真正发挥出玻璃效果
  - 把dock-panel的滚动禁用，让单词页面自己管理滚动，解决排版混乱问题
  - 卡片、选项、反馈面板都添加极致的玻璃质感：多层边框、内高光、外阴影、高强度blur
  - 所有元素加伪元素高光层，增强玻璃的通透和立体感
  - 反馈面板移回vocab-scroll里，解决遮挡选项的问题
  - 暗色模式同步升级，背景用深色渐变+玻璃元素

## v0.0.35 - 2026-05-13
### 更新内容
- 修复对错音效不生效问题
  - 修复AudioContext被浏览器挂起导致无声（增加resume()唤醒）
  - 提高音效音量（gain从0.1提升至0.18），错误音改用triangle波更清晰
  - 页面首次点击自动解锁音频上下文
- 修复继续按钮位置靠上
  - 容器底部内边距增加至16px，选项区底部间隙增加至20px
  - 底部flex间隙从10px提升至16px，按钮行增加上边距
- 液态玻璃效果大幅增强
  - 卡片：rgba 0.85 + blur(32px) saturate(220%)，阴影翻倍
  - 选项：rgba 0.72 + blur(16px) saturate(180%)
  - 反馈面板：rgba 0.82 + blur(30px) saturate(220%)
  - 暗色模式同步增强

## v0.0.34 - 2026-05-13
### 更新内容
- 雅思单词页面全面重构优化
  - 修复继续按钮位置靠上问题，反馈面板移至底部紧邻继续按钮
  - 对错反馈仿不背单词风格重做：大图标+单词音标+释义+例句独立展示
  - 增加对错音效（Web Audio API 生成短促提示音，正确升调/错误降调）
  - 替换切换动画为缩放+淡入淡出组合，更加流畅自然
  - 增强液态玻璃效果：背景透明度提高至0.78，模糊提升至26px
  - 修复单词重复问题：改为随机队列洗牌算法，确保200词全部轮完才重复
- TTS语音进一步优化
  - 优先选择Google在线语音（最自然），其次回退到系统语音
  - Google语音速率0.9/音调1.0，非Google语音速率0.95/音调1.1减少机械感
  - 语音选择结果localStorage持久化，避免重复查找

## v0.0.33 - 2026-05-13
### 更新内容
- 雅思单词系统全面优化
  - 排版重新设计，模拟不背单词/百词斩风格，干净白底无悬浮效果
  - TTS语音优化，自动选择最自然英文语音，语速更真实
  - 增加对错数量记录（localStorage持久化），正确率进度条显示
  - 卡片滑入/滑出过渡动画，提升交互流畅度
  - 选项改为2列网格布局，答案正确/错误边框颜色反馈
- 清理遗留旧代码
  - 移除旧的 toggleAIChat 无用函数
  - 删除所有旧AI模板相关的翻译键（aiWelcome、enterYourQuestion、send）
  - 删除旧AI气泡CSS样式（.ai-msg）
  - 删除Taylor Swift画廊旧代码（initTSGallery）
- 修复Git合并冲突导致网站崩溃
  - 修复4处残留的合并冲突标记（CSS/HTML/JS），页面恢复正常
- 雅思单词页面液态玻璃风格重做
  - 发音按钮从emoji改为SVG喇叭图标+声波动画+液态玻璃容器
  - TTS语音优选12种自然语音（Google UK Female/Microsoft Zira等），语速0.85音调1.05
  - 去掉例句朗读，只朗读单词本身
  - 卡片/选项/反馈面板全部改为液态玻璃效果（backdrop-filter毛玻璃）
  - 选项点击水波纹动画+正确弹性弹跳+错误抖动反馈
  - 对错反馈标题区分显示（正确/答案是）
  - 分数数字点击弹性放大动画

## v0.0.32 - 2026-05-12
### 更新内容
- 雅思词汇库全面升级
  - 将原有初中水平基础词汇全面替换为雅思高频考点单词
  - 词库扩充至200+个真正的雅思核心词汇
  - 词汇涵盖 abandon 到 yield 等雅思必备词汇
  - 每个单词均包含标准音标、英文例句及中文翻译

## v0.0.31 - 2026-05-03 16:30
### 更新内容
- Taylor Swift签名动画系统全面升级
  - 替换所有旧的 .idol- CSS动画逻辑
  - 新增全局8秒循环磨砂与真实手写时间轴
  - 磨砂层淡入->保持->淡出循环动画
  - SVG签名三笔路径分别绑定不同时间轴
  - 所有卡片打乱动画时间，避免同时播放
  - 删除旧的delay类名，统一使用stroke-1/stroke-2/stroke-3命名
- Jennie专题页完善
  - 6张Jennie卡片从文本标签替换为三笔路径SVG
  - 与Taylor Swift保持一致的签名动画效果

## v0.0.30 - 2026-05-03 16:00
### 更新内容
- Taylor Swift专题页视觉与架构全面重构
  - 删除所有旧的 .ts- 开头CSS样式
  - 新增双人专辑展示墙样式（.idol- 命名空间）
  - 引入Google Fonts Great Vibes手写体
  - 专辑卡片hover时缩放+磨砂玻璃遮罩效果
  - SVG签名描边动画+实心填充淡入
- 代码清理优化
  - 删除全部Taylor Swift画廊JavaScript代码
  - 移除二级菜单相关废弃函数调用
  - 替换干净的switchDockTab函数
  - 代码架构更加清晰

## v0.0.29 - 2026-05-03 15:30
### 更新内容
- Taylor Swift专题页交互升级
  - 签名手写动画进入专题页时重新播放，并每隔数秒循环播放
  - 12张专辑海报改为按时间倒序展示（最新专辑在前）
  - 每张专辑支持点击进入详情页
  - 专辑详情页新增专辑封面、时期照片、专辑故事、歌曲列表、背景故事
  - 专辑封面和详情照片加入动态漂移动画

## v0.0.28 - 2026-05-03 15:00
### 更新内容
- Taylor Swift专题页升级为完整12张录音室专辑海报墙
  - 新增evermore、Midnights、The Tortured Poets Department、The Life of a Showgirl
  - 顶部Taylor Swift签名改为模拟真实手写描边动画
  - 专辑卡片加入真实封面图、海报式排版、渐入和悬停过渡
  - 新增公开现场照片区域，增强专题页视觉层次
- 更新"我的"页面版本号为v0.0.28

## v0.0.27 - 2026-05-03 14:00
### 更新内容
- AI聊天全面替换为Taylor Swift专题画廊
  - 移除DeepSeek AI聊天及API密钥
  - 新增Taylor Swift签名SVG标题
  - 8张专辑卡片画廊（Debut至folklore）
  - 每张卡片渐入动画+悬停放大效果
  - 专辑专属渐变色+SVG装饰图标
- 全面代码审计修复9项Bug
- 修复聊天输入框在iOS上位置异常
- 移除所有AI相关代码

## v0.0.26 - 2026-05-03 12:00
### 更新内容
- 修复PC浏览器打开空白页问题
- 修复iOS灵动岛/刘海屏区域视觉适配
- 修复登录时间不更新问题
- 修复注册时间/登录时间显示为"-"的问题
- iOS Safari浏览器完整适配
- 修复底部导航栏/通知/Toast在iOS刘海屏下位置异常

## v0.0.25 - 2026-05-03 10:35
### 更新内容
- 统一公告列表/详情/更新日志的样式大小（字体/间距都统一跟更新日志一致）

## v0.0.24 - 2026-05-03 10:20
### 更新内容
- 彻底修复头像查询：所有头像查询强制加 actor_key=__avatar__，彻底排除旧数据干扰
- 修复手机底部导航往上飘（position:fixed+适配安全区域）

## v0.0.23 - 2026-05-03 10:00
### 更新内容
- 修复公告发布失败bug（不用title列，JSON存content）
- 修复点击头像/个人资料显示旧头像（maybeSingle->limit(1)+上传先删后插，杜绝重复记录）
- 修复聊天列表加载慢（limit 1000->200，缓存30秒->120秒）

## v0.0.22 - 2026-05-03 09:50
### 更新内容
- 修复其他用户看不到最新头像（loadAvatarsForUsers排序取最新）
- 修复底部导航栏可被滑动问题（touch-action禁止手势）
- 彻底去掉页面右侧竖滑动条（html/body overflow:hidden）
- 修复登录时间不更新bug（每次打开页面刷新登录时间）

## v0.0.21 - 2026-05-03 09:30
### 更新内容
- 修复头像过一会儿自动回退bug（localStorage权威优先，DB不再覆盖）
- 去掉评论头像，只显示名字

## v0.0.20 - 2026-05-03 09:20
### 更新内容
- 修复聊天列表打开空白/加载慢问题
- 聊天列表后台预加载，点开秒出
- 彻底去掉帖子列表右侧竖滑动条
- 修复帖子滑动卡顿/抽搐抖动（仅淡入一次+图片加载优化）

## v0.0.19 - 2026-05-03 09:10
### 更新内容
- 修复刷新网页后头像回退bug
- 头像照片压缩进一步减小（80x80 @0.4）
- 修复更换头像后不更新的bug
- 帖子划入划出动画重设计：淡入+上移、淡出+下移
- 去掉帖子和评论的hover悬浮效果

## v0.0.18 - 2026-05-03 08:30
### 更新内容
- 修复更换头像后不更新的bug（彻底修复）
- 去掉底部导航栏点击时的黑色框（彻底修复）
- 帖子加载动画从滑入改成淡入
- 修复注册时间与登录时间相同的bug（彻底修复）
- 头像上传压缩优化（128x128）

## v0.0.17 - 2026-05-02 17:00
### 更新内容
- 动画效果减半优化
  - 帖子滑入动画速度减半，translateY距离减半
  - 所有按钮hover动画幅度减半（底部导航栏除外）
  - 包括hover上浮、缩放、旋转等动画均减半

## v0.0.16 - 2026-05-02 16:53
### 更新内容
- 头像点击行为优化
  - 点击帖子和评论中的头像不再直接跳转聊天
  - 新增用户资料卡片弹窗，显示头像、用户名、最近登录时间
  - 资料卡片中点击"发消息"按钮才跳转到聊天对话
- 统计版块加载速度优化
  - 统计数据增加30秒内存缓存，二次打开秒出
  - 后台预加载统计数据，首次打开也更快
- 聊天功能头像显示
  - 用户聊天消息增加双方头像显示
  - 聊天列表显示联系人真实头像
  - AI对话中显示用户真实头像

## v0.0.15 - 2026-05-02 16:30
### 更新内容
- 头像上传压缩优化
  - 头像上传前自动压缩至256x256，JPEG质量0.7
  - 大幅减少base64体积，防止存储溢出和加载失败
  - 上传大小限制放宽至10MB
- 用户注册/登录时间彻底修复
  - 重构用户信息存取为统一saveUserInfo函数
  - update失败时自动fallback到delete+insert
  - 管理员登录同样正确记录登录时间
  - 后台帖子计数排除用户信息记录
- 数据库RLS策略完善
  - 新增fix_user_info_rls.sql确保UPDATE/DELETE策略存在
  - 扩大actor_key和content长度限制

## v0.0.14 - 2026-05-02 16:20
### 更新内容
- 头像上传导致的连锁问题修复
  - 修复上传头像后帖子页一直显示"加载失败，刷新重试"的严重bug（根因：base64头像数据撑爆localStorage配额）
  - 修复头像base64数据导致localStorage缓存溢出、页面崩溃
  - 修复"我的页面"头像不显示的问题
  - 修复退出登录后旧缓存干扰的问题
  - 优化数据查询，从Supabase查询时排除头像/用户信息记录，大幅减少响应体积