-- ============================================================================
-- 024 编号占位迁移（无实际 DDL）
-- ----------------------------------------------------------------------------
-- 背景（审计 07c-infra-build.md 🔴）：迁移编号链在 023 → 025 之间存在缺口，
-- 历史从未提交过 024 号迁移。为避免后续维护者对"为什么缺 024"产生困惑，
-- 也为了保持编号链连续可回放，本文件以占位注释补齐缺口。
--
-- 本迁移不含任何 SQL 操作，可安全执行；执行后无任何效果。
-- ============================================================================

-- (占位) 无内容。
